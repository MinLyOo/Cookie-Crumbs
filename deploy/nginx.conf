# ============================
# 🍪 Cookie Crumbs Nginx 配置
# 内网测试 / 无域名场景
# 公网使用时将 server_name 改为域名
# ============================

# 访客端 — http://<服务器IP>
server {
    listen 80;
    server_name _;

    root /var/www/cookiecrumbs/visitorapp;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;

        location /assets/ {
            expires 30d;
            add_header Cache-Control "public, immutable";
        }
    }

    location /api/ {
        proxy_pass http://127.0.0.1:30001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
        proxy_send_timeout 30s;
    }

    location /uploads/ {
        proxy_pass http://127.0.0.1:30001;
        proxy_set_header Host $host;
    }

    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    access_log /var/log/nginx/cookiecrumbs-visitor.log;
    error_log /var/log/nginx/cookiecrumbs-visitor-error.log;
}

# 管理端 — http://<服务器IP>:8080
server {
    listen 8080;
    server_name _;

    root /var/www/cookiecrumbs/adminapp;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;

        location /assets/ {
            expires 30d;
            add_header Cache-Control "public, immutable";
        }
    }

    location /api/ {
        proxy_pass http://127.0.0.1:30001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
        proxy_send_timeout 30s;
    }

    location /uploads/ {
        proxy_pass http://127.0.0.1:30001;
        proxy_set_header Host $host;
    }

    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    access_log /var/log/nginx/cookiecrumbs-admin.log;
    error_log /var/log/nginx/cookiecrumbs-admin-error.log;
}
